{
  "name": "pi-agent-bus-bridge",
  "version": "0.1.0",
  "type": "commonjs",
  "description": "Pi Extension to bridge pi-agent-bus-node agents with pi.tools and pi-link",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/test/*.test.js",
    "test:watch": "node --test --watch dist/test/*.test.js"
  },
  "dependencies": {
    "pi-agent-bus-node": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
